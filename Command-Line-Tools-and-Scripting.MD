####Search for a file  
```find ~/ -type f -name "filename"``` to find a file  
```find ~ -type d -name "dirname"``` to find a directory.  
```find .``` to search the current directory.  
```find ~/``` will search your entire home directory.  
```sudo find /``` to search the whole filesystem.
